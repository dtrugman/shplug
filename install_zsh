set -eu

git clone "https://github.com/dtrugman/shplug.git" "$HOME/.shplug/repos/shplug"

rcfile="$HOME/.zshrc"
if [[ ! -f "$rcfile" ]] || ! grep -q 'source "$HOME/.shplug/repos/shplug/shplug.sh"' "$rcfile"; then
    printf '\n[[ -s "$HOME/.shplug/repos/shplug/shplug.sh" ]] && source "$HOME/.shplug/repos/shplug/shplug.sh"' >> "$rcfile"
fi
